#!/usr/bin/env python
# -*- coding: utf-8 -*-

'''
reads and processes radar data
'''

def loadSpectra(loadSample=True,dataPath=None):
    '''
    load Doppler spectra data

    loadSample: 
        if True: load processed data from sample_data directory
        if False: load data from path
    '''
    #import xarray as xr
    #from os import path
    
    #import pdb; pdb.set_trace()
    #module_path = path.split(path.abspath(PSDretrieval.__file__))[0]

    #if loadSample:
    #    xrSpec = xr.open_dataset("sample_data/20190130_1430.nc")
    #else:
    #    if dataPath is None:
    #        print("error: provide path to radar data if loadSample=False")
    #        sys.exit(0)

    print("TODO") 
